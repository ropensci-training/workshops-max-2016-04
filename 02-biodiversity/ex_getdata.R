###########
# Pick a taxonomic name, any name, and get a data.frame back with occurrence data

myname = "?????" # (e.g., 'Abies concolor')

## GBIF, via rgbif package


## any sources, via spocc


###########
# Do another query but tweak parameters, try something different
# e.g,. you could try
# - change how many records with the `limit` parameter
# - with `rgbif`, query within a certain geographic area
# - do the same with `spocc`
# - how to get back just data with coordinate values?

